/* Feature flags.
 *
 * Diet logging (calories/protein, food entry, rings) is fully built and working,
 * but hidden for now — the user is only tracking workouts at the moment.
 * Flip DIET_ENABLED to true to bring it all back: the diet card on Today / the
 * Calendar day view, plus the diet ring and "diet on target" legend on the
 * Calendar month grid. Nothing else needs to change. */
export const DIET_ENABLED = false;
